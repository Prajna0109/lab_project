name: CI Workflow

on: [push, pull_request]
  # push:
  #   branches:
  #     - main  # Fix the spacing here

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run tests
        run: |
          echo "Running tests"
          # Add your testing commands here

  # test:
  #   runs-on: ubuntu-latest

  #   steps:
  #     - name: Checkout code  # Fix the spacing here
  #       uses: actions/checkout@v2

  #     - name: Install dependencies
  #       run: npm install

  #     - name: Run tests
  #       run: npm test  # Fix indentation here
